#include <stdio.h>
/**
 * main - program that prints lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
