#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length os a string
 *
 * @s: character to get string
 * Return: return length of a string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}
	return (i);
}
